The alternative file is a concatenate of the filename that is supplied in the system-file and the word “alternative”. So there is no reason to change the system.xml file if alternative is needed. Alternative file should only contain the values that is different from the standard file’s data.